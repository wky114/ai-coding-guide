# Codex 教程

本目录收录 **Codex**（命令行 AI 编码工具）的小白教程。

- 文章从 `01` 起**独立编号**，与 `claude-code/` 互不干扰。
- 章节规划见仓库根目录 `ROADMAP.md` 的「Codex」板块（待规划）。
- 写作规范与 Claude Code 篇**共用**根目录 `CLAUDE.md`；配图按 `assets/<编号-主题>/` 存放。
